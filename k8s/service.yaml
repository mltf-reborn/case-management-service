apiVersion: v1
kind: Service
metadata:
  name: case-management-service
  labels:
    app: case-management-service
spec:
  type: NodePort
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: http
  selector:
    app: case-management-service
